# Next.js Public Environment Variables
# These are exposed to the browser, so use NEXT_PUBLIC_ prefix

# API Backend URL - CHANGE THIS to your DigitalOcean server URL
NEXT_PUBLIC_API_URL=https://api.your-backend-domain.com/api